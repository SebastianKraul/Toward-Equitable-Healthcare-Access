# Analysis + figures (reproduce.py needs only these)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
openpyxl>=3.1

# Optional — only to regenerate the optimization outputs (src/optimization.py).
# Gurobi is commercial and requires a license:
#   gurobipy>=11
# License-free alternative: swap in HiGHS (highspy) inside optimization.py.
