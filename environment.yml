name: equitable-healthcare
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - pandas>=2.0
  - numpy>=1.24
  - scikit-learn>=1.3
  - matplotlib>=3.7
  - openpyxl>=3.1
  # Optional, only to regenerate optimization outputs (requires a Gurobi license):
  # - pip
  # - pip:
  #     - gurobipy>=11
